The uploaded index.html file also contains the CSS and JS code. Since it was a basic cover page related to a company, all the code has been integrated into one file.
Install ngrok on your system to access the chatbot
